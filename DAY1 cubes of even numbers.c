#include <stdio.h>
int main()
{
    int cubesum(int n)
    {
        int sum = 0;
        for(int i = 1; i <= n; i++)
            sum += (2 * i) * (2 * i)
                * (2 * i);

        return sum;
    }



     int public static void Main();
    {
        int a = 8;
        Console.WriteLine(cubesum(a));

    }
}
